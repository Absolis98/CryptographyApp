import React from 'react';
import { View, StyleSheet, Text, Button } from 'react-native';

const DecipherInfoScreen = ({ navigation }) => {
    return (
        <View>
            <Text>Hello there!</Text>
            <Text>DecipherInfoScreen</Text>
        </View>
    )
}

export default DecipherInfoScreen;